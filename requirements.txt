# ============================================================
# requirements.txt — Hotel Intelligence v10.0
# Rifyal Tumber · MTT · 2025
# ============================================================
# Install: pip install -r requirements.txt
# Letakkan sejajar dengan template.py di root repo
# ============================================================

streamlit>=1.38.0

pandas>=2.1.0
numpy>=1.26.0
plotly>=5.18.0
requests>=2.31.0

# Excel reader (wajib - fallback engine)
openpyxl>=3.1.2

# Excel writer untuk download button
xlsxwriter>=3.1.0

# Excel reader CEPAT - 3-8x lebih cepat (v10 baru)
# Jika tidak bisa install, app auto-fallback ke openpyxl
python-calamine>=0.5.0

# Parquet cache - instant load file yang sama (v10 baru)
pyarrow>=14.0.0
